package appliances;

/**
 * Enum representing different types of devices in the smart home.
 */
public enum StuffType {
    LIGHT,
    TV,
    FRIDGE,
    AC,
    DOOR,
    SECURITY_CAMERA,
    THERMOSTAT,
    AUDIO_SYSTEM
}